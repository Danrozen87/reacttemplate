
/**
 * @description Type definitions for auth form translations
 */
export interface AuthFormTranslations {
  auth: {
    loginSuccess: string;
    loginError: string;
    tryAgain: string;
    roleAssigned: {
      admin: string;
      manager: string;
      user: string;
    };
  };
}
