
export interface PasswordRecoveryFormTranslations {
  auth: {
    recovery: {
      title: string;
      description: string;
      email: string;
      emailPlaceholder: string;
      submit: string;
      submitting: string;
      backToLogin: string;
    };
  };
}
