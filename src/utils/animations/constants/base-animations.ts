
export const baseAnimations = {
  fade: {
    in: "animate-fade-in",
    out: "animate-fade-out",
  },
  scale: {
    in: "animate-scale-in",
    out: "animate-scale-out",
  },
  slide: {
    in: "animate-slide-in",
    out: "animate-slide-out",
  },
} as const;
